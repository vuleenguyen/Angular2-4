export class LoggingService {

    logActiveToInActive(counter : number) {
        console.log("Counter from Active To InActive: " + counter);
    }

    logInActiveToActive(counter: number) {
        console.log("Counter from InActive to Active: " + counter);
    }
}